<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fscommunity.platform.persist.dao.AgendaRoomDao">

    <sql id="fields">
    id,
    article_id as articleId,
    title as title,
    create_time as createTime,
    update_time as updateTime
    </sql>

    <insert id="saveAgendaRoom" parameterType="com.fscommunity.platform.persist.pojo.AgendaRoomInfo">
    insert into
    agenda_room (
    article_id,
    title
    )
    values
    (
    #{articleId},
    #{title})
      ON DUPLICATE KEY UPDATE
      title=VALUES(title),
      article_id=VALUES(article_id)
    </insert>

    <select id="queryById" parameterType="java.lang.Integer"
      resultType="com.fscommunity.platform.persist.pojo.AgendaRoomInfo">
        select
        <include refid="fields"/>
        from agenda_room
        where id=#{id}
    </select>

    <select id="countAgendaRoom" resultType="java.lang.Integer">
        select count(1) from agenda_room
        <where>
            <if test="fuzzyName != null and fuzzyName !=''">
                title like concat('%',#{fuzzyName},'%')
            </if>
        </where>
        ;
    </select>
    <select id="list" resultType="com.fscommunity.platform.persist.pojo.AgendaRoomInfo">
        select
        <include refid="fields"/>
        from agenda_room
        <where>
            <if test="fuzzyName != null and fuzzyName !=''">
                title like concat('%',#{fuzzyName},'%')
            </if>
        </where>
    </select>

    <update id="updateAgendaRoom" parameterType="com.fscommunity.platform.persist.pojo.AgendaRoomInfo">
        update agenda_room
        <set>
            article_id=#{articleId},
            <if test="title != null and title != ''">
                title=#{title},
            </if>
        </set>
        where id=#{id}
    </update>

    <delete id="delById" parameterType="java.lang.Integer">
        delete from agenda_room
        where id=#{id}
    </delete>
</mapper>